//defining a class for images
class Image {
    constructor(uploadDate, fileName, fileSize, fileURL) {
        this.uploadDate = uploadDate;
        this.fileName = fileName;
        this.fileSize = fileSize;
        this.fileURL = fileURL;
    }
}